[project]
name = "mailwarden"
version = "0.1.0"
description = "Rule-based spam filter for Gmail and IMAP mailboxes"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mailwarden*"]
